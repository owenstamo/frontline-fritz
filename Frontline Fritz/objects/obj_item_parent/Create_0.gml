gravity = item_gravity;

// Default for whether player can pick up or not
can_pick_up = false;

// Hspeed of player when item is dropped
drop_speed_x = 0;

// Whether or not the object is hidden and in the HiddenInstances layer
hidden = false;

// If the item was dropped last frame. Exists to ensure player doesn't re-pick it up
just_dropped = false;